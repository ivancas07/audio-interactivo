# Instrucciones de Configuración Manual en Notion

Para que el script funcione, necesitamos crear las siguientes columnas en tu base de datos **"Audio Interactivo 2026"**.

**IMPORTANTE:** El nombre de la columna NO debe llevar paréntesis. El paréntesis indica el TIPO de dato que debes seleccionar en el menú de Notion.

## Lista de Propiedades (Columnas)

| Nombre de Columna (Property Name) | Tipo (Type) |
| :--- | :--- |
| **Semana** | Number (#) |
| **Fecha** | Date (📅) |
| **Tipo** | Select (▼) |
| **Tema** | Text (Aa) |
| **Resumen** | Text (Aa) |
| **Slug** | Text (Aa) |
| **Publicado** | Checkbox (☑) |
| **Año** | Number (#) |
| **Clave** | Text (Aa) (Opcional) |

### Pasos:
1. En tu base de datos, haz clic en el símbolo `+` a la derecha de la última columna.
2. Escribe el **Nombre** (ej: `Semana`).
3. En **Type**, selecciona la opción correcta (ej: `Number`).
4. Repite para todas las columnas de la lista.
